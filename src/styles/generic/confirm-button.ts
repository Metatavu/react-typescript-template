import { makeStyles } from "@material-ui/core";

export const useConfirmButtonStyles = makeStyles({

  loaderContainer: {
    width: "100%",
    height: 200,
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  }

}, {
  name: "confirm-button"
});